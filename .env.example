# The live app is the Next.js project in `nextjs/`.
# Copy and configure environment variables there:
#
#   cd nextjs
#   copy .env.example .env
#
# Required: DB_*, AUTH_SECRET, APP_URL_NEXT, and MAIL_* for email.
